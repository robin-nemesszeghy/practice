// Values to convert
const stringValue = '42';
const zeroNumber = 0;
const positiveNumber = 7;
const emptyString = '';
const boolValue = true;
const numValue = 123;

// TODO: Convert stringValue to a boolean and log the result
let bool1 = Boolean(stringValue);
console.log("Boolean('42'): ", bool1);

// TODO: Convert zeroNumber to a boolean and log the result
let bool2 = Boolean(zeroNumber);
console.log("Boolean(0): ", bool2);

// TODO: Convert positiveNumber to a boolean and log the result
let bool3 = Boolean(positiveNumber);
console.log("Boolean(7): ", bool3);

// TODO: Convert emptyString to a boolean and log the result
let bool4 = Boolean(emptyString);
console.log("Boolean(''): ", bool4);

// TODO: Convert boolValue to a string and log the result
let str1 = String(boolValue);
console.log("String(true): ", str1);

// TODO: Convert numValue to a string and log the result
let str2 = String(numValue);
console.log("String(123): ", str2);
